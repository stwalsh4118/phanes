# 9-2 Create config.yaml.example

[Back to task list](./tasks.md)

## Description

Create example configuration file with all options documented with inline comments explaining each option's purpose and valid values.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-01-27 00:00:00 | Created | N/A | Proposed | Task file created | sean |
| 2025-01-27 00:00:00 | Status Change | Proposed | InProgress | Started implementation | ai-agent |
| 2025-01-27 00:00:00 | Status Change | InProgress | Review | config.yaml.example created with all options documented | ai-agent |
| 2025-01-27 00:00:00 | Status Change | Review | Done | Task completed and approved | sean |

## Requirements

1. All configuration sections documented:
   - `user`: username, ssh_public_key
   - `system`: timezone
   - `swap`: enabled, size
   - `security`: ssh_port, allow_password_auth
   - `docker`: install_compose
   - `postgres`: enabled, version, password, database, user
   - `redis`: enabled, password, bind_address
   - `nginx`: enabled
   - `caddy`: enabled
   - `devtools`: enabled, node_version, python_version, go_version, install_uv
   - `coolify`: enabled
2. Comments explaining each option's purpose and valid values
3. Sensible defaults shown (matching DefaultConfig())

## Implementation Plan

1. Create config.yaml.example at project root
2. Include all configuration sections with inline YAML comments
3. Document purpose and valid values for each option
4. Use sensible defaults matching DefaultConfig()

## Verification

1. config.yaml.example documents all options
2. Comments explain "why" not just "what"
3. Defaults match DefaultConfig() function
4. File is valid YAML

## Files Modified

- `config.yaml.example` (new) - Example configuration file with all options documented
- `docs/api-specs/infrastructure/infrastructure-api.md` (updated) - Updated config struct field documentation

